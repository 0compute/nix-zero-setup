[project]
name = "strata-ml"
version = "0.1.0"
description = "Strata ML: A heavy, layered ML environment example for nix-zero-setup"
dependencies = [
    "torch",
    "numpy",
    "pandas",
]
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

